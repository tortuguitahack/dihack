# Docker removed

This repository used to include Docker configurations for demo purposes. Per project policy, Docker support has been removed and the system is designed to run entirely on local services with credentials kept in a single local .env file.

If you previously relied on docker-compose.yml, it has been replaced with this notice. Run services locally as described in README.md.
